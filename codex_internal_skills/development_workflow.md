# Development Workflow Skill

When building this repo:

1. Keep changes modular and commit after each verified feature slice.
2. Write tests before deterministic analysis behavior.
3. Keep transcript content and generated outputs in `local_data/`.
4. Prefer declarative study skills over arbitrary runtime code.
5. Run backend tests and frontend build/lint before claiming completion.

